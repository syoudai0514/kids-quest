# Claudeレビュー依頼: PR #22 育成最小縦切り

以下をそのままClaude Codeへ渡す。レビュー対象のPR・headを固定し、レビュー中の実装変更を禁止することで、指摘と修正を混ぜない。

---

GitHubリポジトリ `syoudai0514/kids-quest` の Draft PR #22 を、次の固定headで独立レビューしてください。

- PR: https://github.com/syoudai0514/kids-quest/pull/22
- base: `main` / `99cd50459c3170b39891e7a5084de75ea02a8403`
- head: `terra/monster-growth-release` / `d4b919c41f834025cfc93703deaf71a51665c683`
- 関連: Issue #9、Issue #7、`docs/monster-growth-release.md`
- 実装担当: Codex Terra
- 独立レビュー担当: Claude（保存・PWA・子ども向け導線は高リスクとして厳密に確認）

## レビューの目的

画像1000体の量産を再開する前に、図鑑番号51〜62（内部ID `g042`〜`g053`）だけで、子どもが「学習する → 戦う → 捕まえる → 育てる → 進化/形態解放する」を安全に遊べるか確認する。

今回のPRは次を含む。

- 敵捕獲、最大3体のチーム、出場個体の選択
- 学習XP、バトルXP、Lv.1〜99、`g042→g043`進化
- `g052`スター覚醒、`g053`ギガスター（3攻撃）とボス固有技の予告
- 図鑑の育成詳細表示
- 既存saveからv4への移行

## 固定条件

次の既存契約を変更・弱体化・初期化してはいけない。

1. 基本バトルは1日3戦。
2. バトルチケットの有効期限は7日。
3. 追加問題のチケット条件は正答率2/3以上かつ`!suspicious`。
4. 既存のプロフィール、学習進捗、図鑑、SRS、英語、武器、当日ミッションを保持する。
5. `g063`〜`g087`とPR #21は次回の画像更新であり、PR #22に混ぜない。
6. 画像を自動で`asset-approved`にしない。

## 必須手順

1. 作業開始時に `AGENTS.md`、PR #22、Issue #9、`docs/monster-growth-release.md` を読む。
2. 固定headをcheckoutし、PR本文や実装者の説明を正解とみなさずコード・テスト・差分から独立に確認する。
3. レビュー中はコード変更、commit、push、merge、Issue/PRのcloseを行わない。
4. まず対象テスト、続いて `npm run check` と `git diff --check` を実行する。実行不能なら、コマンド・失敗理由・未確認範囲を明記する。
5. 既存保存を実データ同様に扱い、保存を消す・resetする・schemaを都合よく置換する検証はしない。

## 重点レビュー観点

### A. save v4移行・複数プロフィール

- v1〜v3、v4、プロフィール封筒、未知の`unlockedMonsters` ID、空/壊れたparty、既存チケットを確認する。
- `companions`が不変の`companion:<捕獲時monsterId>`であり、進化後も捕獲元identityが保たれるか。
- migrationが冪等で、既存のXP/図鑑/SRS/英語/武器/当日ミッションを失わないか。
- 1000体を捕獲した保存が、端末localStorageで現実的なサイズか。

### B. 報酬・不正対策・既存学習契約

- 通常問題、復習、追加問題で学習XPが意図どおりか。
- `suspicious`な追加問題では、回答途中も含めて育成XP・スターゲージが増えないか。
- 同じタスクの再レンダー/再dispatchで報酬が二重に入らないか。
- 追加問題の2/3チケット条件、基本3戦、7日チケットが回帰していないか。

### C. バトル状態機械

- 捕獲・勝利・敗北が一度だけ記録されるか。タイマーやstate更新で二重記録されないか。
- 交代時、敵HP、敵レベル、ボス予告、形態の対戦中使用権が不正にリセットされないか。
- `g053`ボスは予告後に強攻撃となり、まもる/交代が有効な選択になるか。
- スター覚醒は通常個体identityを保ち、ギガスターは3回の攻撃後に終了し、専用技が扱われるか。
- 捕獲済みの再戦、partyが1体/3体、未捕獲優先出現も確認する。

### D. 5歳向けUI/PWA

- 幅375pxと393px相当で、図鑑詳細、チーム選択、バトルの技/まもる/交代/形態が押せるか。切れ、重なり、safe area、戻れない状態を確認する。
- チームが1体のとき「外す」を押して無反応にならないか。満員時の説明が分かるか。
- PWA buildとservice worker生成が成功し、今回のリリースに不要な`g063`〜`g087`を取り込んでいないか。

## 返却形式

PR #22のレビューコメントまたはユーザーへ、必ず次の形で返してください。

```markdown
## PR #22 独立レビュー

判定: APPROVE / REQUEST CHANGES
確認head: d4b919c41f834025cfc93703deaf71a51665c683
実行済み: <コマンドと結果>
未実行: <なければ「なし」>

### 指摘

| 優先度 | 場所 | 事実・再現手順 | 利用者への影響 | 修正案 | 必要なテスト |
|---|---|---|---|---|---|
| P0〜P3 | path:line | ... | ... | ... | ... |

### 固定条件の確認

- 基本3戦: PASS / FAIL（根拠）
- 7日チケット: PASS / FAIL（根拠）
- 追加問題2/3・suspicious: PASS / FAIL（根拠）
- 既存save/進捗保持: PASS / FAIL（根拠）
- 375px/393px: PASS / FAIL / 未確認（根拠）

### 総評

<P0/P1が0件でも、残るP2/P3とリリース判断を明記>
```

P0またはP1が1件でもあれば`REQUEST CHANGES`。P0/P1が0件で、未実行項目がリリース判断に影響しない場合だけ`APPROVE`とする。曖昧な印象ではなく、path・line・再現手順・テスト根拠で記載する。

---

## 返却後の処理（Codex用）

1. P0/P1はTerraが修正、対象テスト→`npm run check`→`git diff --check`の順で確認する。
2. P2/P3は、今回直すか次回Issueへ送るかを根拠つきで決める。
3. 修正をまとめて1回だけClaudeへ再レビュー依頼する。画像量産のレビューはこの機能PRのmerge後まで再開しない。
