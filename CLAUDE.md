@AGENTS.md

## Claude Code固有メモ

- 共通ルールとKids Quest固有ルールの正本は `AGENTS.md`。同じ内容をこのファイルへ重複記載しない。
- セッション開始時に `/context` で `AGENTS.md` が読み込まれていることを確認できる。
- 固定タイミングで必ず実行すべき処理は、会話上の約束だけでなくテスト・CI・hook化を提案する。
